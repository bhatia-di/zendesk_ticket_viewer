export function isNull (object) {
    return object === null;
}


export function isNonNull (object) {
    return object !== null;
}

export function isArrayEmpty (array) {
    return array.length == 0;
}